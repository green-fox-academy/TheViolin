let typo = "Chinchill";

function appendAFunc (typo){
    if (typo ===""){
        return typo = "add a parameter";
    }
    {
        return typo= typo + "a";
    }
}
console.log (appendAFunc (typo));